# Keeping uploaded images

Superseded by **[pushing-without-losing-images.md](pushing-without-losing-images.md)**.

That one is the current instruction, and it matters: the images were lost
twice. The short version is that `node push.mjs <store>.myshopify.com` is now
the way to push, and it cannot touch anything the theme editor owns.
